# Flask-billing-project
flask billing system with Db
BREW & BEANS -Flask Billing System
A simple system built using Flask, SQLite with HTML,CSS interface. And CRUD operations, Billing logic, backend integration.
